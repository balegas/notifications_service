[
  {antidote_notifications, []},
  {kernel,
    [{logger,
      [{handler, default, logger_std_h,
        #{level => notice,
          config => #{file => "log/antidote_notifications.log"}}},
        {handler, debug, logger_std_h,
          #{level => debug,
            config => #{type => standard_io}}}
      ]},
      {logger_level, debug}
    ]
  }
].
