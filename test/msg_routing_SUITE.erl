-module(msg_routing_SUITE).

-export([
  init_per_suite/1,
  end_per_suite/1
  %init_per_testcase/2,
  %end_per_testcase/2,
  %all/0
]).

%% API
-export([]).


init_per_suite(_Config) -> ok.
end_per_suite(_Config) -> ok.

